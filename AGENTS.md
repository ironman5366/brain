A repo for research and exploration of models trained on EEG data.

The root of the repo is for server-side AI research, meant to run in a GPU environment. Things at client/ are meant to run on the laptop connected to the EEG headset.